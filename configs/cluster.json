{
    "__default__" :
    {
        "mem"    : 100,
        "runtime"   : 10,
        "name"      : "JOBNAME.{rule}.{wildcards}",
        "output"    : "logs/cluster/{wildcards.study}/{rule}.{wildcards}.out",
        "error"     : "logs/cluster/{wildcards.study}/{rule}.{wildcards}.err"
    }
}
